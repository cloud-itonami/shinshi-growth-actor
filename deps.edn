{:paths ["src"]
 ;; langgraph brings langchain transitively (both zero-/single-dep,
 ;; all .cljc → portable to JVM / SCI / ClojureScript / GraalVM).
 ;; (2026-07 org-wide *-clj rename: orgs/kotoba-lang/*)
 :deps {io.github.kotoba-lang/langgraph
        {:local/root "../../kotoba-lang/langgraph"}
        io.github.kotoba-lang/langchain
        {:git/sha "06a27bf4efef1efe758042c4c5f26500b2c56b30"}}
 :aliases
 {;; offline workspace dev — pin transitive langchain to local checkout
  :dev {:override-deps
        {io.github.kotoba-lang/langchain
         {:local/root "../../kotoba-lang/langchain"}}}
  ;; clojure -M:dev:run  → drive the demo through one OperationActor
  :run {:main-opts ["-m" "growth.sim"]}
  ;; clojure -M:dev:run-live  → Phase 2: same OperationActor, seeded from LIVE
  ;; club-shinshi metrics (growth.live, growth.facts) instead of demo-data.
  ;; http-kit/jsonista here only (langchain.jvm's own zero-dep-by-default
  ;; convention — see its docstring) — growth.facts itself stays dep-free.
  :run-live {:extra-deps {http-kit/http-kit {:mvn/version "2.8.0"}
                          metosin/jsonista  {:mvn/version "0.3.11"}}
             :main-opts  ["-m" "growth.live"]}
  :test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  ;; static analysis — clj-kondo from deps (no binary), errors fail CI.
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts    ["-m" "clj-kondo.main"
                        "--lint" "src" "test"
                        "--fail-level" "error"]}}}
